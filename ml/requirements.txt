torch>=2.0          # neural network framework
numpy>=1.24         # array operations and .npz storage
pandas>=2.0         # convenient JSONL loading with pd.read_json
matplotlib>=3.7     # plotting training curves and card win rates
tensorboard>=2.13   # live training metrics dashboard (launch: tensorboard --logdir runs/)
tqdm>=4.65          # progress bars for preprocessing and training loops
